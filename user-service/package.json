{
  "name": "user-service",
  "version": "1.0.0",
  "description": "Microservicio de usuarios para EventFlow",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest",
    "docker:redis": "docker run -d -p 6379:6379 --name redis-eventflow redis:7-alpine",
    "docker:mongo": "docker run -d -p 27017:27017 --name mongo-eventflow -e MONGO_INITDB_DATABASE=eventflow mongo:5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.18.2",
    "redis": "^5.8.2"
  },
  "devDependencies": {
    "@types/redis": "^4.0.10",
    "jest": "^29.6.4",
    "nodemon": "^3.1.10"
  }
}
